#include <enet/enet.h>
#include <stdio.h>
#include <raylib.h>
static const int screenWidth = 1080;
static const int screenHeight = 720;
int main(){
    /*
    if (enet_initialize () != 0)
    {
        fprintf (stderr, "An error occurred while initializing ENet.\n");
        return EXIT_FAILURE;
    }
    InitWindow(screenWidth, screenHeight, "streetninja");
     */
    
    
    return 0;
    
}
